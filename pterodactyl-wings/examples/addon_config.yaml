# Pterodactyl Wings - Example Home Assistant Add-on Configuration

# This is the Home Assistant add-on configuration (set via "Configuration" tab)
# The actual Wings daemon configuration is placed in /share/pterodactyl/config.yml

# Path to the Wings daemon configuration file.
# The Panel add-on automatically generates this file at /share/pterodactyl/config.yml.
# If you are running Wings standalone, create your own config.yml here.
config_file: /share/pterodactyl/config.yml
