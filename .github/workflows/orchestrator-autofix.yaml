name: Orchestrator Auto-Fix

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  autofix:
    name: 🔧 Auto-Fix & Format
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: ⤵️ Check out code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT || secrets.GITHUB_TOKEN }}

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: 📦 Install Python Deps
        run: pip install pyyaml

      - name: 🏷️ Auto-Generate OCI Labels
        run: python3 .scripts/fix_oci_labels.py

      - name: ⚠️ Update Unsupported Status
        run: python3 .scripts/update_unsupported_status.py

      - name: 🏷️ Update README Beta Status
        run: python3 .scripts/update_readme_status.py

      - name: 🛡️ Enforce 64-bit Architectures
        id: enforce_arch
        run: python3 .scripts/enforce_architectures.py

      - name: 💬 Comment on PR (32-bit Removal)
        if: github.event_name == 'pull_request' && steps.enforce_arch.outputs.modified == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const signature = "<!-- autofix-32bit-notice -->";

            const { data: comments } = await github.rest.issues.listComments({
               owner: context.repo.owner,
               repo: context.repo.repo,
               issue_number: context.issue.number,
            });

            const previousComments = comments.filter(c => c.user.type === 'Bot' && (c.body.includes(signature) || c.body.includes("**Notice:** 32-bit architectures")));

            for (const comment of previousComments) {
                try {
                    await github.graphql(`
                        mutation($subjectId: ID!) {
                            minimizeComment(input: {subjectId: $subjectId, classifier: OUTDATED}) {
                                minimizedComment { isMinimized }
                            }
                        }
                    `, { subjectId: comment.node_id });
                } catch (error) {
                    console.error(`Failed to minimize comment ${comment.id}:`, error);
                }
            }

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: signature + '\n⚠️ **Notice:** 32-bit architectures (armhf, armv7, i386) have been automatically removed from this PR. These architectures are no longer supported by Home Assistant or this repository.'
            })

      - name: 🏁 Renormalize Line Endings
        run: git add --renormalize .

      # 1. Shell Scripts (Format)
      - name: 🐚 Setup shfmt
        uses: mfinelli/setup-shfmt@v4
        with:
          shfmt-version: "3.7.0"

      - name: 🐚 Run shfmt
        run: shfmt -l -w .

      # 2. Python (Format & Sort Imports)
      # We format the internal scripts and any add-on python files
      - name: 🐍 Run Black (Formatter)
        uses: psf/black@stable
        with:
          options: "--verbose"
          src: "."

      - name: 🐍 Run Isort (Import Sorter)
        uses: isort/isort-action@master
        with:
          configuration: "--profile black"

      # 3. Prettier (JSON, YAML, MD)
      - name: 💅 Run Prettier
        continue-on-error: true
        run: |
          npm install -g prettier
          prettier --write "**/*.{json,js,md,yaml}" --ignore-path .prettierignore

      # 4. Standardize (READMEs, Beta Labels, Configs)
      - name: 📄 Standardize READMEs
        run: python3 .scripts/standardize_readmes.py

      # 5. Markdown Fixes (markdownlint)
      - name: 📝 Fix Markdown
        continue-on-error: true
        run: |
          npm install -g markdownlint-cli
          markdownlint --fix "**/*.md" || true

      # Commit back
      - name: 💾 Commit Changes
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: "style: auto-fix (shfmt, black, isort, prettier, markdownlint)"
          branch: ${{ github.head_ref || github.ref_name }}
          file_pattern: .
          # Use PAT to trigger downstream workflows (CI)
          github_token: ${{ secrets.PAT || secrets.GITHUB_TOKEN }}
